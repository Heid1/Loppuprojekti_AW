<!DOCTYPE html>
<html>
<head>
    <title>Add Map</title>

    <style type="text/css">
        /* Set the size of the div element that contains the map */
        #map {
            height: 600px;
            /* The height is 400 pixels */
            width: 100%;
            /* The width is the width of the web page */
        }
    </style>
    <script>
        // Initialize and add the map
        function initMap() {
            // The location of Uluru
            const kamppi = { lat: 60.167390, lng: 24.931080 };
            const kamppi2 = { lat: 60.167555, lng: 24.83200 };
            const kamppi3 = { lat: 60.177555, lng: 24.83200 };
            // The map, centered at Uluru
            const map = new google.maps.Map(document.getElementById("map"), {
                zoom: 11.5,
                center: kamppi,
            });
            

            var icon = {
                url: "https://image.flaticon.com/icons/png/512/94/94107.png", // url
                scaledSize: new google.maps.Size(50, 50), // scaled size
                origin: new google.maps.Point(0, 0), // origin
                anchor: new google.maps.Point(0, 0) // anchor
            };
            var icon2 = {
                url: "https://cdn0.iconfinder.com/data/icons/dance-fitness/68/06-512.png", // url
                scaledSize: new google.maps.Size(50, 50), // scaled size
                origin: new google.maps.Point(0, 0), // origin
                anchor: new google.maps.Point(0, 0) // anchor
            };

            var icon3 = {
                url: "https://www.hectortenis.com/wp-content/uploads/2017/07/padel.png", // url
                scaledSize: new google.maps.Size(50, 50), // scaled size
                origin: new google.maps.Point(0, 0), // origin
                anchor: new google.maps.Point(0, 0) // anchor
            };

            // The marker, positioned at Helsinki
            const marker = new google.maps.Marker({
                position: kamppi,
                map: map,
                icon: icon
            });
            const marker2 = new google.maps.Marker({
                position: kamppi2,
                map: map,
                icon: icon2
            });

            const marker3 = new google.maps.Marker({
                position: kamppi3,
                map: map,
                icon: icon3
            });

            var hello = "hullo kaikille";
            const infowindow = new google.maps.InfoWindow({
                content: hello,
            });
            marker.addListener("click", () => {
                map.setZoom(15);
                map.setCenter(marker.getPosition());
            });
            marker.addListener("click", () => {
                infowindow.open(marker.get("map"), marker);
            });
            
        }
    </script>
</head>
<body>
    <h3>My Google Maps Demo</h3>
    <!--The div element for the map -->
    <div id="map"></div>

    <!-- Async script executes immediately and must be after any DOM elements used in callback. -->
    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyB3QpAniVaOsNKERSCcBjkoon3Iojbbxhw&callback=initMap&libraries=&v=weekly"
            async></script>
</body>
</html>